<h3>Reviews of <em><%= @movie.title %></em></h3>
<% if current_user %>
  <% @movie.reviews.each do |review| %>
    <p>Comments: <%= review.text %></p>
    <p>Rating: <%= review.rating_out_of_ten %>
    <% if review.rating_out_of_ten %>
    /10
    <% end %>
    </p>
    <small>Reviewed by- <%= link_to review.user.full_name, user_path(review.user) if review.user %></small><br/>
  <% end %>
  <p><%= link_to "Write a review!", new_movie_review_path(@movie) %></p>
<% else %>
  <p>Please <%= link_to "log in", new_session_path %> to see reviews and add your own.</p> 
<% end %>